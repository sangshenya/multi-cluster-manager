plugins:
  inTree:
  - name: etcd
    configuration:
      <plugin configuration object>
  - name: apiserver
    configuration:
      <plugin configuration object>
  outTree:
  - name: external-plugin
    http:
      url: <http url>